{
    "ErrorExceptions": [
        {
            "ValidationTest": "API Validation",
            "ExceptionMessage": "Breaking changes require a new major version.",
            "Comment": "New parameters have default values so it won't break compiling existing code. We were unable to find a workaround for this error.",
            "PackageVersion": "2.3.0"
        }
    ],
    "WarningExceptions": []
}
